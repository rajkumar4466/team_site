---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - src/components/Navbar.tsx
  - src/app/page.tsx
  - src/app/players/page.tsx
  - src/app/stats/page.tsx
  - src/app/gallery/page.tsx
autonomous: true
requirements:
  - SITE-01
  - SITE-02
  - SITE-03

must_haves:
  truths:
    - "User sees a KKR-branded homepage with a hero section in purple/gold"
    - "User can navigate to Players, Stats, and Gallery from the navbar on any page"
    - "Navbar collapses to a mobile-friendly hamburger menu on small screens"
    - "Site is readable on desktop (1280px) and mobile (375px) without horizontal scroll"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Site-wide responsive navigation"
      exports: ["Navbar"]
    - path: "src/app/layout.tsx"
      provides: "Root layout wrapping all pages with Navbar"
      contains: "Navbar"
    - path: "src/app/page.tsx"
      provides: "Homepage with KKR hero section"
      contains: "kkr-purple"
    - path: "src/app/players/page.tsx"
      provides: "Players placeholder page"
    - path: "src/app/stats/page.tsx"
      provides: "Stats placeholder page"
    - path: "src/app/gallery/page.tsx"
      provides: "Gallery placeholder page"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/Navbar.tsx"
      via: "import and render"
      pattern: "import.*Navbar"
    - from: "src/components/Navbar.tsx"
      to: "/players, /stats, /gallery"
      via: "Next.js Link component"
      pattern: "href.*players|href.*stats|href.*gallery"
    - from: "src/app/page.tsx"
      to: "tailwind.config.ts color tokens"
      via: "Tailwind class names"
      pattern: "kkr-purple|kkr-gold"

interfaces:
  # KKR color tokens from 01-01-PLAN (tailwind.config.ts):
  # kkr-purple: #3A1078
  # kkr-gold: #FFD700
  # kkr-purple-light: #5B2EA6
  # kkr-purple-dark: #260B52
  # Usage: bg-kkr-purple, text-kkr-gold, border-kkr-gold, etc.
---

<objective>
Build the site shell: root layout with KKR-branded responsive navbar, homepage with hero section, and placeholder pages for Players/Stats/Gallery.

Purpose: Delivers all three Phase 1 requirements — SITE-01 (branded homepage), SITE-02 (navigation), SITE-03 (responsive). After this plan the site is navigable and on-brand.
Output: Navigable multi-page site shell deployed as static HTML.
</objective>

<execution_context>
@/Users/mithra_sundaram/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mithra_sundaram/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the KKR Navbar component with responsive mobile menu</name>
  <files>
    src/components/Navbar.tsx
  </files>
  <action>
    Create `src/components/Navbar.tsx` — a responsive navigation bar with KKR branding.

    Requirements:
    - KKR logo/wordmark on the left: "KKR" in bold text using kkr-gold on kkr-purple background
    - Navigation links on the right: Players, Stats, Gallery
    - Desktop: links visible inline
    - Mobile: hamburger icon (three lines) toggles a dropdown menu with the same three links
    - Uses Next.js `Link` component (not `<a>`) for client-side navigation
    - Active link detection using `usePathname()` from `next/navigation` — highlight the current section
    - Mobile menu state managed with `useState` — close on link click

    Implementation:

    ```tsx
    'use client'

    import Link from 'next/link'
    import { usePathname } from 'next/navigation'
    import { useState } from 'react'

    const navLinks = [
      { href: '/players', label: 'Players' },
      { href: '/stats', label: 'Stats' },
      { href: '/gallery', label: 'Gallery' },
    ]

    export function Navbar() {
      const pathname = usePathname()
      const [menuOpen, setMenuOpen] = useState(false)

      return (
        <nav className="bg-kkr-purple text-white sticky top-0 z-50 shadow-lg">
          <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div className="flex items-center justify-between h-16">
              {/* Logo */}
              <Link href="/" className="flex items-center gap-2">
                <span className="text-kkr-gold font-extrabold text-2xl tracking-wider">KKR</span>
                <span className="hidden sm:block text-white text-sm font-medium opacity-80">
                  Kolkata Knight Riders
                </span>
              </Link>

              {/* Desktop nav */}
              <div className="hidden md:flex items-center gap-8">
                {navLinks.map((link) => (
                  <Link
                    key={link.href}
                    href={link.href}
                    className={`text-sm font-semibold uppercase tracking-wide transition-colors duration-150
                      ${pathname.startsWith(link.href)
                        ? 'text-kkr-gold border-b-2 border-kkr-gold pb-0.5'
                        : 'text-white hover:text-kkr-gold'
                      }`}
                  >
                    {link.label}
                  </Link>
                ))}
              </div>

              {/* Mobile hamburger */}
              <button
                className="md:hidden p-2 rounded text-white hover:text-kkr-gold focus:outline-none"
                onClick={() => setMenuOpen(!menuOpen)}
                aria-label="Toggle menu"
              >
                <div className="w-6 h-0.5 bg-current mb-1.5"></div>
                <div className="w-6 h-0.5 bg-current mb-1.5"></div>
                <div className="w-6 h-0.5 bg-current"></div>
              </button>
            </div>
          </div>

          {/* Mobile dropdown */}
          {menuOpen && (
            <div className="md:hidden bg-kkr-purple-dark border-t border-kkr-gold/20">
              {navLinks.map((link) => (
                <Link
                  key={link.href}
                  href={link.href}
                  onClick={() => setMenuOpen(false)}
                  className={`block px-6 py-3 text-sm font-semibold uppercase tracking-wide
                    ${pathname.startsWith(link.href)
                      ? 'text-kkr-gold bg-kkr-purple/50'
                      : 'text-white hover:text-kkr-gold hover:bg-kkr-purple/30'
                    }`}
                >
                  {link.label}
                </Link>
              ))}
            </div>
          )}
        </nav>
      )
    }
    ```
  </action>
  <verify>
    <automated>cd /Users/mithra_sundaram/Desktop/code/AI/projects/team_site && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/components/Navbar.tsx exists
    - TypeScript compiles without errors on this file
    - Component exports named `Navbar`
    - Contains Links to /players, /stats, /gallery
    - Contains mobile menu toggle logic
  </done>
</task>

<task type="auto">
  <name>Task 2: Create root layout, homepage hero, and placeholder section pages</name>
  <files>
    src/app/layout.tsx
    src/app/globals.css
    src/app/page.tsx
    src/app/players/page.tsx
    src/app/stats/page.tsx
    src/app/gallery/page.tsx
  </files>
  <action>
    **src/app/globals.css** — Add Google Fonts import for Inter and ensure Tailwind directives are present:

    ```css
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

    @tailwind base;
    @tailwind components;
    @tailwind utilities;

    @layer base {
      body {
        @apply bg-white text-gray-900 font-sans;
      }
    }
    ```

    **src/app/layout.tsx** — Root layout wrapping every page with the Navbar and a footer:

    ```tsx
    import type { Metadata } from 'next'
    import './globals.css'
    import { Navbar } from '@/components/Navbar'

    export const metadata: Metadata = {
      title: 'KKR — Kolkata Knight Riders',
      description: 'Your home for KKR squad profiles, season stats, and player action photography.',
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html lang="en">
          <body>
            <Navbar />
            <main>{children}</main>
            <footer className="bg-kkr-purple-dark text-white/60 text-center py-6 text-sm mt-16">
              <p>Kolkata Knight Riders Fan Site &copy; {new Date().getFullYear()}</p>
              <p className="mt-1 text-xs">This is an unofficial fan site. Not affiliated with KKR or the BCCI.</p>
            </footer>
          </body>
        </html>
      )
    }
    ```

    **src/app/page.tsx** — Homepage with KKR hero section:

    ```tsx
    import Link from 'next/link'

    export default function HomePage() {
      return (
        <div>
          {/* Hero Section */}
          <section className="bg-kkr-purple text-white py-24 px-4 text-center relative overflow-hidden">
            {/* Background accent */}
            <div className="absolute inset-0 bg-gradient-to-br from-kkr-purple via-kkr-purple to-kkr-purple-dark opacity-90" />

            <div className="relative max-w-4xl mx-auto">
              <p className="text-kkr-gold text-sm font-semibold uppercase tracking-widest mb-4">
                Kolkata Knight Riders
              </p>
              <h1 className="text-5xl sm:text-6xl lg:text-7xl font-extrabold tracking-tight mb-6">
                The Heart of{' '}
                <span className="text-kkr-gold">Kolkata</span>
              </h1>
              <p className="text-lg sm:text-xl text-white/80 max-w-2xl mx-auto mb-10">
                Explore the full KKR squad, relive iconic seasons, and discover the
                stories behind the purple and gold.
              </p>
              <div className="flex flex-col sm:flex-row gap-4 justify-center">
                <Link
                  href="/players"
                  className="bg-kkr-gold text-kkr-purple-dark font-bold px-8 py-3 rounded-lg text-sm uppercase tracking-wide hover:brightness-110 transition-all"
                >
                  Meet the Squad
                </Link>
                <Link
                  href="/stats"
                  className="border-2 border-kkr-gold text-kkr-gold font-bold px-8 py-3 rounded-lg text-sm uppercase tracking-wide hover:bg-kkr-gold hover:text-kkr-purple-dark transition-all"
                >
                  Season Stats
                </Link>
              </div>
            </div>
          </section>

          {/* Section cards */}
          <section className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
            <h2 className="text-2xl font-bold text-kkr-purple text-center mb-10">
              Explore KKR
            </h2>
            <div className="grid grid-cols-1 sm:grid-cols-3 gap-6">
              {[
                {
                  href: '/players',
                  title: 'Squad',
                  desc: 'Profiles, bios, and jersey numbers for every KKR player.',
                  emoji: '👕',
                },
                {
                  href: '/stats',
                  title: 'Stats',
                  desc: 'Season-by-season records and all-time KKR top performers.',
                  emoji: '📊',
                },
                {
                  href: '/gallery',
                  title: 'Gallery',
                  desc: 'Action shots and memorable moments from the pitch.',
                  emoji: '📷',
                },
              ].map((card) => (
                <Link
                  key={card.href}
                  href={card.href}
                  className="group block bg-white border border-gray-200 rounded-xl p-6 hover:border-kkr-gold hover:shadow-lg transition-all"
                >
                  <div className="text-3xl mb-3">{card.emoji}</div>
                  <h3 className="text-lg font-bold text-kkr-purple group-hover:text-kkr-purple-light mb-2">
                    {card.title}
                  </h3>
                  <p className="text-gray-600 text-sm">{card.desc}</p>
                </Link>
              ))}
            </div>
          </section>
        </div>
      )
    }
    ```

    **Placeholder pages** — Create minimal on-brand placeholders for the three sections. Each signals "coming soon" while matching the site's visual style. Create the same pattern for all three:

    `src/app/players/page.tsx`:
    ```tsx
    export default function PlayersPage() {
      return (
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 text-center">
          <h1 className="text-4xl font-extrabold text-kkr-purple mb-4">KKR Squad</h1>
          <p className="text-gray-500 text-lg">Player profiles coming soon.</p>
        </div>
      )
    }
    ```

    `src/app/stats/page.tsx`:
    ```tsx
    export default function StatsPage() {
      return (
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 text-center">
          <h1 className="text-4xl font-extrabold text-kkr-purple mb-4">Season Stats</h1>
          <p className="text-gray-500 text-lg">Historical stats and records coming soon.</p>
        </div>
      )
    }
    ```

    `src/app/gallery/page.tsx`:
    ```tsx
    export default function GalleryPage() {
      return (
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-16 text-center">
          <h1 className="text-4xl font-extrabold text-kkr-purple mb-4">Gallery</h1>
          <p className="text-gray-500 text-lg">Action photography coming soon.</p>
        </div>
      )
    }
    ```

    After creating all files, run a full build to confirm static export produces all four pages.
  </action>
  <verify>
    <automated>cd /Users/mithra_sundaram/Desktop/code/AI/projects/team_site && npm run build 2>&1 | tail -15 && ls out/ 2>/dev/null</automated>
  </verify>
  <done>
    - `npm run build` exits 0
    - out/ contains index.html, players/index.html, stats/index.html, gallery/index.html
    - TypeScript compiles without errors (`npx tsc --noEmit` exits 0)
    - Navbar renders on all pages (imported in layout.tsx)
    - Homepage hero uses kkr-purple background and kkr-gold accent text
  </done>
</task>

</tasks>

<verification>
Run `npm run build` — must exit 0. Run `npx tsc --noEmit` — must exit 0. Inspect out/ to confirm 4 HTML pages exist (index, players, stats, gallery). Grep for kkr-purple in page.tsx to confirm branding applied.
</verification>

<success_criteria>
- Navbar appears on all pages with Players, Stats, Gallery links
- Homepage has a KKR-branded hero section (purple background, gold accents, headline)
- All three placeholder section pages exist at /players, /stats, /gallery
- Site has no horizontal scroll at 375px viewport width (responsive)
- `npm run build` produces static HTML in out/ with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` with:
- Components created and their key implementation details
- Confirmation of static build output (list files in out/)
- Any responsive issues encountered and how resolved
- Color tokens used (which kkr-* classes appear in which files)
</output>
