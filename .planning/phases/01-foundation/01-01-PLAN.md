---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tailwind.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - src/app/globals.css
  - .gitignore
autonomous: true
requirements:
  - SITE-03

must_haves:
  truths:
    - "Next.js project exists and compiles without errors"
    - "Tailwind CSS is configured with KKR purple and gold as named color tokens"
    - "Static export is configured so the site can be deployed as a CDN-hosted bundle"
  artifacts:
    - path: "package.json"
      provides: "Next.js + Tailwind dependencies"
      contains: "next, tailwindcss"
    - path: "next.config.ts"
      provides: "Static export configuration"
      contains: "output: 'export'"
    - path: "tailwind.config.ts"
      provides: "KKR brand color tokens"
      contains: "kkr-purple, kkr-gold"
    - path: "src/app/globals.css"
      provides: "Base Tailwind directives"
      contains: "@tailwind base"
  key_links:
    - from: "tailwind.config.ts"
      to: "src/app/globals.css"
      via: "Tailwind content scan"
      pattern: "content.*src"
    - from: "next.config.ts"
      to: "build output"
      via: "output: 'export'"
      pattern: "output.*export"
---

<objective>
Bootstrap the KKR fan website as a Next.js project with Tailwind CSS and configure it for static export.

Purpose: All subsequent plans build on this scaffold. Getting the toolchain and KKR color tokens right here means every component inherits the brand without per-file color hacks.
Output: A working Next.js + Tailwind project with KKR purple/gold tokens and static export enabled.
</objective>

<execution_context>
@/Users/mithra_sundaram/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mithra_sundaram/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js project with Tailwind CSS</name>
  <files>
    package.json
    next.config.ts
    tsconfig.json
    postcss.config.mjs
    src/app/globals.css
    .gitignore
  </files>
  <action>
    Bootstrap a Next.js 14+ project using the App Router in the current directory (/Users/mithra_sundaram/Desktop/code/AI/projects/team_site).

    Run the following to scaffold:
    ```
    npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-git
    ```

    After scaffolding, verify the project structure looks like:
    - src/app/layout.tsx
    - src/app/page.tsx
    - src/app/globals.css
    - tailwind.config.ts
    - next.config.ts

    The create-next-app command initializes Tailwind automatically. Do NOT add any additional CSS frameworks.
  </action>
  <verify>
    <automated>cd /Users/mithra_sundaram/Desktop/code/AI/projects/team_site && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Project compiles without errors. `npm run build` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Configure KKR brand colors and static export</name>
  <files>
    tailwind.config.ts
    next.config.ts
  </files>
  <action>
    **tailwind.config.ts** — Extend the theme with KKR brand color tokens so every component can use `bg-kkr-purple`, `text-kkr-gold`, etc.:

    ```typescript
    import type { Config } from 'tailwindcss'

    const config: Config = {
      content: [
        './src/pages/**/*.{js,ts,jsx,tsx,mdx}',
        './src/components/**/*.{js,ts,jsx,tsx,mdx}',
        './src/app/**/*.{js,ts,jsx,tsx,mdx}',
      ],
      theme: {
        extend: {
          colors: {
            'kkr-purple': '#3A1078',   // KKR deep purple
            'kkr-gold': '#FFD700',     // KKR gold/yellow
            'kkr-purple-light': '#5B2EA6',
            'kkr-purple-dark': '#260B52',
          },
          fontFamily: {
            sans: ['Inter', 'sans-serif'],
          },
        },
      },
      plugins: [],
    }

    export default config
    ```

    **next.config.ts** — Enable static export for Vercel CDN deployment (no server runtime needed):

    ```typescript
    import type { NextConfig } from 'next'

    const nextConfig: NextConfig = {
      output: 'export',
      trailingSlash: true,
      images: {
        unoptimized: true,  // Required for static export (no Next.js image server)
      },
    }

    export default nextConfig
    ```

    Note: `images.unoptimized: true` is required because static export cannot use Next.js image optimization which requires a server.

    After editing, run a build to confirm static export works. The build output should be in the `out/` directory.
  </action>
  <verify>
    <automated>cd /Users/mithra_sundaram/Desktop/code/AI/projects/team_site && npm run build 2>&1 | tail -10 && ls out/ 2>/dev/null | head -5</automated>
  </verify>
  <done>
    - `npm run build` exits 0
    - `out/` directory exists containing at minimum `index.html`
    - tailwind.config.ts contains `kkr-purple` and `kkr-gold` color tokens
  </done>
</task>

</tasks>

<verification>
npm run build must exit 0. out/ directory must exist. tailwind.config.ts must define kkr-purple and kkr-gold.
</verification>

<success_criteria>
- Next.js App Router project is scaffolded and compiles clean
- KKR brand color tokens (kkr-purple: #3A1078, kkr-gold: #FFD700) defined in Tailwind config
- Static export produces out/ directory on `npm run build`
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` with:
- What was scaffolded and the exact commands run
- The KKR color token values defined
- Confirmation that static export is working
- Any deviations from this plan and why
</output>
